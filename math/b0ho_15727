import java.util.*;

public class Main {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int answer = 0;
		int dist = sc.nextInt();

		answer += dist / 5;
		dist %= 5;
		if (dist != 0) {
			answer++;
		}

		System.out.println(answer);

	}
}
